//requires at least the full event --this done before calling the function!
//checks if any users in waiting list
//pull user from waiting list (assing to var)
//push user to participant list (id from var)
//gets user info (var id)
//send attending email (event, user.email)\\const { Event } = require("../models/Event");
// const { User } = require("../models/User");
// const bcrypt = require("bcrypt");
// const {
//   passRecoveryEmail,
//   eventSignUpEmail,
//   eventCancellationEmail,
// } = require("../API/mailer/mailer");

// export default function pushWaitingList(event) {
//     if (event)
// }
